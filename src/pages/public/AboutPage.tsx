import { Navbar } from "../../components/layout/Navbar";

export const About = () => {
    return (
        <>
            <Navbar />
            <div className="min-h-screen bg-background text-on-background">
                {/* Content rendered by AboutPage — placeholder for future */}
            </div>
        </>
    );
};
